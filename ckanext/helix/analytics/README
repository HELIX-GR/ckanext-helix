You must set the following properties in order for the plugin to work correctly:

```
ckanext.helix.analytics.rasdaman_endpoint = http://example.org/rasdaman/ows
ckanext.helix.analytics.api_endpoint = http://example.org:5000/api/analytics
ckanext.helix.analytics.log_pattern = /mnt/hgfs/logs/*.log
ckanext.helix.analytics.database_conn_url = postgresql://pman:pass@localhost/analyticsdb
```
